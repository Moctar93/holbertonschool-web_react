// Define a type RowID which is a number
export type RowID = number;

// Define an interface RowElement with optional age
export interface RowElement {
    firstName: string;
    lastName: string;
    age?: number;
}

